# ChromaDB with IPv6 support for Railway - Minimal approach
FROM ghcr.io/chroma-core/chroma:latest

# Just set the environment variable to bind to all interfaces
ENV CHROMA_HOST_ADDR=0.0.0.0

# The original image should handle everything else